; Expected: modus_ponens rejects an explicit argument.
; Tested with Ethos; no Lean build was run here.
(declare-const p Bool)
(declare-const q Bool)
(assume @p p)
(assume @imp (=> p q))
(step @q q :rule modus_ponens :premises (@p @imp) :args (p))
